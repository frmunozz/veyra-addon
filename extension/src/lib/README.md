# Lib helpers

Store small, page-agnostic utilities here (DOM helpers, throttle/debounce, storage helpers). Keep them dependency-free so they can be copied directly into content/background scripts without a build step.
